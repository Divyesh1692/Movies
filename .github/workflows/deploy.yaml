# The name of the project.
name: Movies-backend

# The version of the project.
version: 1.0.0

# The description of the project.
description: A backend for a movie website.

# The author of the project.
author: Divyesh1692

# The dependencies of the project.
dependencies:
  express: "^4.18.1"
  mongoose: "^6.1.1"

# The scripts for the project.
scripts:
  start: node index.js
  test: jest

# The environment variables for the project.
environment:
  NODE_ENV: production

# The deployment configuration for the project.
deployment:
  provider: github
  repo: Movies
  branch: main
  deploy_path: /
